#!/usr/bin/node

// Define a class Rectangle
class Rectangle {

    // initialize an instance with these values
    constructor (w, h) {
        // constroctor with w and h paramiters
        this.width = w;
        this.height = h;
    }
}

module.exports = Rectangle;